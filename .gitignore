# Runtime log from th07_coop.dll — pure run output; a stale committed copy once
# sent a session chasing a phantom bug (handoff §5b). Never commit it.
build/coop_log.txt

# Generated test artifacts from tests/run_netsim.sh
build/clean_host.csv
build/clean_guest.csv
build/desync_host.csv
build/desync_guest.csv
# netsim driver is a build product, not a shipped artifact
build/netsim.exe
# 2-process handshake smoke test binary (source in tests/, build locally)
build/handshake_test.exe

# Assembled beta drop (staging folder + zip) from package_release.ps1.
# Built from committed sources (src/launcher, release/*); not a tracked artifact.
build/release/

# host-native test binary produced by build.sh (not a Windows deliverable)
build/merge_test_native

# build.ps1 packs the intermediate objects here; not a shipped artifact
build/obj.zip

# tester-supplied 2-PC log/trace dumps dropped under build/ — pure run output,
# same as coop_log.txt; analyzed in place, never committed.
build/new test logs/

# Build products regenerated by build.sh/build.ps1 (not shipped deliverables).
# th07_coop_net.dll is the standalone netcode-test DLL (build.sh only); coop.o is
# the coop.c object from the netcode-into-coop.c link; the .ini files are config
# templates the build scripts auto-create if absent.
build/th07_coop_net.dll
build/obj/coop.o
build/coop.ini
build/coop_net.ini

# Copyrighted game binary — used locally for RE/disasm, NEVER commit it
th07.exe
